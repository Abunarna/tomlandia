-- V7 healing food release, step 1/3: stage the v7 content (inactive).
--
-- GENERATED by scripts/v7/build-migrations.mjs from
-- artifacts/v7/supabase/generated/content-manifest.sql and
-- supabase/v7/food-runtime.sql. Do not edit this file directly.
-- Content manifest sha256: 89789521c5b84948af1b954e86fdd9d6bea127d8315d3bb5b4f553d340a46935
--
-- v7 = live v6 content plus an enumerated healing delta. Every unchanged row is
-- copied from v6 inside the database, so it cannot drift; the changed rows are
-- updated in place:
--   * 16 dishes get their approved healing amount
--   * names, ids, values, recipes, ingredients, quantities, Cooking
--     requirements, XP, durations and output quantities are unchanged
--   * 0 items deleted, converted or re-identified
-- Manual eating is repaired generically at the same time: it resolves the dish
-- from the active runtime catalogue, exactly like auto-eat already does.

BEGIN;

DO $v7_stage_content_guard$
BEGIN
  IF (SELECT active_content_version FROM public.game_content_control WHERE singleton) <> 'v6' THEN
    RAISE EXCEPTION 'V7 staging expects v6 to be the active release';
  END IF;
  IF EXISTS (SELECT 1 FROM public.game_content_versions WHERE content_version = 'v7' AND status = 'active') THEN
    RAISE EXCEPTION 'V7 is already active; staging must not run again';
  END IF;
END
$v7_stage_content_guard$;

INSERT INTO public.game_content_versions
  (content_version, spawn_set_version, uuid_namespace, manifest_hash, status, starter_loadout, mechanics, player_notice)
VALUES ('v7', 'v7', 'bf50882c-ad8a-57ab-bb73-3ea3dd8fcb5c', '89789521c5b84948af1b954e86fdd9d6bea127d8315d3bb5b4f553d340a46935', 'staged', '{"armor_item_id":"copper_light_armor","plus":0,"weapon_item_id":"copper_sword"}'::jsonb, '{"approved_balance_model_hash":"e1fbe19aac61014b38885ce38cd16d9a12e3852f24858301a2588c65fba4a640","defense_multiplier_rule":"1 + 0.1% * plus","fishing_xp_curve":[{"level_requirement":1,"tier_index":1,"xp_per_action":3},{"level_requirement":10,"tier_index":2,"xp_per_action":6},{"level_requirement":20,"tier_index":3,"xp_per_action":17},{"level_requirement":30,"tier_index":4,"xp_per_action":40},{"level_requirement":40,"tier_index":5,"xp_per_action":102},{"level_requirement":50,"tier_index":6,"xp_per_action":331},{"level_requirement":60,"tier_index":7,"xp_per_action":588},{"level_requirement":70,"tier_index":8,"xp_per_action":660},{"level_requirement":80,"tier_index":9,"xp_per_action":753},{"level_requirement":90,"tier_index":10,"xp_per_action":844},{"level_requirement":100,"tier_index":11,"xp_per_action":935},{"level_requirement":110,"tier_index":12,"xp_per_action":1025},{"level_requirement":120,"tier_index":13,"xp_per_action":1113},{"level_requirement":130,"tier_index":14,"xp_per_action":1208},{"level_requirement":140,"tier_index":15,"xp_per_action":1312},{"level_requirement":150,"tier_index":16,"xp_per_action":1413}],"gear_resale_rule":"floor(item_value * 0.40 + cumulative_upgrade_spend * 0.15)","healing_foods":[{"heal":15,"item_id":"honey_bun","recipe_id":"cook_honey_bun","tier_index":1,"value":12},{"heal":45,"item_id":"berry_pie","recipe_id":"cook_berry_pie","tier_index":2,"value":34},{"heal":120,"item_id":"hearty_stew","recipe_id":"cook_hearty_stew","tier_index":3,"value":90},{"heal":135,"item_id":"fishermans_stew","recipe_id":"cook_fishermans_stew","tier_index":4,"value":95},{"heal":155,"item_id":"golden_koi_feast","recipe_id":"cook_golden_koi_feast","tier_index":5,"value":100},{"heal":180,"item_id":"sunspiced_eel","recipe_id":"cook_sunspiced_eel","tier_index":6,"value":105},{"heal":210,"item_id":"runic_fish_stew","recipe_id":"cook_runic_fish_stew","tier_index":7,"value":130},{"heal":245,"item_id":"shadow_stew","recipe_id":"cook_shadow_stew","tier_index":8,"value":140},{"heal":300,"item_id":"frost_tonic","recipe_id":"cook_frost_tonic","tier_index":9,"value":180},{"heal":340,"item_id":"wyrm_feast","recipe_id":"cook_wyrm_feast","tier_index":10,"value":210},{"heal":375,"item_id":"phoenix_fillet","recipe_id":"cook_phoenix_fillet","tier_index":11,"value":700},{"heal":445,"item_id":"starsteel_feast","recipe_id":"cook_starsteel_feast","tier_index":12,"value":705},{"heal":485,"item_id":"void_feast","recipe_id":"cook_void_feast","tier_index":13,"value":710},{"heal":525,"item_id":"wyrmforged_feast","recipe_id":"cook_wyrmforged_feast","tier_index":14,"value":715},{"heal":605,"item_id":"ancient_feast","recipe_id":"cook_ancient_feast","tier_index":15,"value":720},{"heal":645,"item_id":"ascendant_feast","recipe_id":"cook_ascendant_feast","tier_index":16,"value":790}],"light_attack_multiplier_rule":"1 + 5% * min(plus, 20) + 1% * max(plus - 20, 0)","market_fee_pct":5,"max_level":150,"max_plus":100,"strength_potions":[{"boost_hits":8,"item_id":"minor_venom_draught","strength_pct":12,"tier_index":1},{"boost_hits":25,"item_id":"bronze_damage_potion","strength_pct":12,"tier_index":2},{"boost_hits":30,"item_id":"goblins_fury_tonic","strength_pct":12,"tier_index":3},{"boost_hits":30,"item_id":"steel_damage_potion","strength_pct":12,"tier_index":4},{"boost_hits":30,"item_id":"serpents_bite_elixir","strength_pct":12,"tier_index":5},{"boost_hits":33,"item_id":"sunsteel_damage_potion","strength_pct":12,"tier_index":6},{"boost_hits":33,"item_id":"runite_damage_potion","strength_pct":12,"tier_index":7},{"boost_hits":35,"item_id":"shadow_venom","strength_pct":13,"tier_index":8},{"boost_hits":35,"item_id":"froststeel_damage_potion","strength_pct":13,"tier_index":9},{"boost_hits":35,"item_id":"wyrmsteel_damage_potion","strength_pct":14,"tier_index":10},{"boost_hits":35,"item_id":"frostfire_brew","strength_pct":15,"tier_index":11},{"boost_hits":35,"item_id":"starsteel_damage_potion","strength_pct":16,"tier_index":12},{"boost_hits":35,"item_id":"voidsteel_damage_potion","strength_pct":16,"tier_index":13},{"boost_hits":35,"item_id":"wyrmforged_damage_potion","strength_pct":17,"tier_index":14},{"boost_hits":35,"item_id":"ancient_damage_potion","strength_pct":17,"tier_index":15},{"boost_hits":35,"item_id":"ascendant_damage_potion","strength_pct":18,"tier_index":16}],"upgrade_cost_rule":"round_to_5(max(25, item_value * (0.08 + 3.4 * sqrt(next_plus))))","weapon_multiplier_rule":"1 + 2% * min(plus, 50) + 0.5% * max(plus - 50, 0)"}'::jsonb, '{"details":["Every tier from level 1 to level 150 still offers exactly one dish, with the same recipe and ingredients.","Healing amounts are rebalanced for the top six dishes so far less healing is wasted on overheal.","Food prices, recipes, ingredients, Cooking requirements, experience and cooking times are unchanged.","Eating from your bag now uses the same authoritative food data as auto-snacking, so every dish works.","Auto-snacking behaviour is unchanged: it still fires before a killing blow is settled and can save you.","No dish is deleted, converted or re-identified, and every holding, listing and price is preserved."],"summary":"All 16 dishes keep their recipes and prices; the highest tiers heal a more useful amount and manual eating works for every dish.","title":"Tomlandia healing food release"}'::jsonb)
ON CONFLICT (content_version) DO NOTHING;

DO $v7_copy_content$
DECLARE
  target text;
  column_list text;
  projection text;
  copied bigint;
  expected bigint;
BEGIN
  FOREACH target IN ARRAY ARRAY['game_content_tiers', 'game_content_items', 'game_content_recipes', 'game_content_recipe_inputs', 'game_content_nodes', 'game_content_monsters', 'game_content_monster_loot', 'game_content_fish', 'game_content_fishing_spots', 'game_content_quests', 'game_content_bosses', 'game_content_progression_levels'] LOOP
    SELECT string_agg(quote_ident(attname), ', ' ORDER BY attnum),
           string_agg(CASE WHEN attname = 'content_version' THEN '''v7'''
                           ELSE quote_ident(attname) END, ', ' ORDER BY attnum)
      INTO column_list, projection
    FROM pg_attribute
    WHERE attrelid = format('public.%I', target)::regclass AND attnum > 0 AND NOT attisdropped;

    EXECUTE format('DELETE FROM public.%I WHERE content_version = %L', target, 'v7');
    EXECUTE format(
      'INSERT INTO public.%I (%s) SELECT %s FROM public.%I WHERE content_version = %L',
      target, column_list, projection, target, 'v6');

    EXECUTE format('SELECT count(*) FROM public.%I WHERE content_version = %L', target, 'v7') INTO copied;
    EXECUTE format('SELECT count(*) FROM public.%I WHERE content_version = %L', target, 'v6') INTO expected;
    IF copied <> expected OR expected = 0 THEN
      RAISE EXCEPTION 'V7 copy of % is incomplete (% of %)', target, copied, expected;
    END IF;
  END LOOP;
END
$v7_copy_content$;

-- ---------------------------------------------------------------------------
-- Healing delta. Nothing is deleted: the 16 stable dishes are updated in place,
-- so no foreign-key parent row is ever dropped and re-inserted.
-- ---------------------------------------------------------------------------
DELETE FROM public.game_content_migration_rules WHERE content_version = 'v7';

UPDATE public.game_content_items AS item
SET heal = updated.heal
FROM (VALUES
  ('honey_bun', 15),
  ('berry_pie', 45),
  ('hearty_stew', 120),
  ('fishermans_stew', 135),
  ('golden_koi_feast', 155),
  ('sunspiced_eel', 180),
  ('runic_fish_stew', 210),
  ('shadow_stew', 245),
  ('frost_tonic', 300),
  ('wyrm_feast', 340),
  ('phoenix_fillet', 375),
  ('starsteel_feast', 445),
  ('void_feast', 485),
  ('wyrmforged_feast', 525),
  ('ancient_feast', 605),
  ('ascendant_feast', 645)
) AS updated(id, heal)
WHERE item.content_version = 'v7'
  AND item.id = updated.id;

INSERT INTO public.game_content_migration_rules
  (content_version, from_id, action, to_id, captured_value_required, notice_key, equipped_action, unequipped_action)
VALUES
  ('v7', 'bronze_dagger', 'stop', NULL, false, 'bronze_dagger_removed', NULL, NULL),
  ('v7', 'cloth_tunic', 'stop', NULL, false, 'cloth_tunic_removed', NULL, NULL),
  ('v7', 'frostguard_plate', 'stop', NULL, false, 'frostguard_plate_removed', NULL, NULL),
  ('v7', 'iron_mail', 'stop', NULL, false, 'iron_mail_removed', NULL, NULL),
  ('v7', 'leather_vest', 'stop', NULL, false, 'leather_vest_removed', NULL, NULL),
  ('v7', 'linen_robe', 'stop', NULL, false, 'linen_robe_removed', NULL, NULL),
  ('v7', 'mithril_plate', 'stop', NULL, false, 'mithril_plate_removed', NULL, NULL),
  ('v7', 'mystic_robe', 'stop', NULL, false, 'mystic_robe_removed', NULL, NULL),
  ('v7', 'runite_plate', 'stop', NULL, false, 'runite_plate_removed', NULL, NULL),
  ('v7', 'sunspire_wand', 'stop', NULL, false, 'sunspire_wand_removed', NULL, NULL),
  ('v7', 'tungsten_bar', 'compensate', NULL, true, 'tungsten_bar_captured_value_compensation', NULL, NULL),
  ('v7', 'tungsten_maul', 'stop', NULL, false, 'tungsten_maul_removed', NULL, NULL),
  ('v7', 'tungsten_ore', 'compensate', NULL, true, 'tungsten_ore_captured_value_compensation', NULL, NULL),
  ('v7', 'wooden_club', 'stop', NULL, false, 'wooden_club_removed', NULL, NULL),
  ('v7', 'wyrmscale_plate', 'stop', NULL, false, 'wyrmscale_plate_removed', NULL, NULL);

-- ---------------------------------------------------------------------------
-- Authoritative manual eating (generic, catalogue-driven; auto-eat unchanged).
-- ---------------------------------------------------------------------------
-- V7 healing-food runtime.
--
-- Canonical source for the authoritative manual-eating entry point. It is
-- inlined verbatim into the generated V7 stage-content migration.
--
-- The repair is generic, never an allowlist: the server resolves the dish from
-- the active runtime catalogue (public.game_runtime_items, which already
-- restricts to the active content version and to active rows), so every food
-- the release publishes works, and anything else is rejected.
--
-- Contract:
--   * authenticated callers only, action gate shared with auto-eat;
--   * the save row is locked FOR UPDATE before any read-modify-write;
--   * healing is capped at max_hp - hp and resolved server-side only;
--   * exactly one item is consumed, atomically, in the same transaction;
--   * a rejection writes nothing at all to the save.
--
-- Auto-eat (public.try_auto_eat) is deliberately untouched by this release.

CREATE OR REPLACE FUNCTION public.consume_food(_index integer)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
#variable_conflict use_variable
DECLARE
  uid uuid := auth.uid();
  data jsonb;
  slot jsonb;
  food_id text;
  heal integer;
  hp integer;
  max_hp integer;
  qty integer;
  healed integer := 0;
BEGIN
  PERFORM public.game_assert_action_allowed(false);
  IF uid IS NULL THEN RAISE EXCEPTION 'not authenticated'; END IF;
  IF _index IS NULL OR _index < 0 OR _index > 19 THEN
    RETURN jsonb_build_object('ok', false, 'reason', 'bad_slot');
  END IF;

  SELECT player_saves.data INTO data FROM public.player_saves WHERE user_id = uid FOR UPDATE;
  IF NOT FOUND THEN RETURN jsonb_build_object('ok', false, 'reason', 'no_save'); END IF;

  slot := data->'inv'->_index;
  IF jsonb_typeof(slot) <> 'object' THEN
    RETURN jsonb_build_object('ok', false, 'reason', 'empty');
  END IF;
  food_id := slot->>'id';
  IF food_id IS NULL THEN RETURN jsonb_build_object('ok', false, 'reason', 'empty'); END IF;

  -- Generic authoritative resolution: active release, active row, real food.
  SELECT item.heal INTO heal
  FROM public.game_runtime_items AS item
  WHERE item.id = food_id AND item.active AND item.kind = 'food' AND item.heal > 0;
  IF NOT FOUND THEN RETURN jsonb_build_object('ok', false, 'reason', 'not_food'); END IF;

  max_hp := public.player_max_hp(data);
  hp := least(max_hp, greatest(0, coalesce((data->>'hp')::integer, max_hp)));
  IF hp >= max_hp THEN
    RETURN jsonb_build_object('ok', false, 'reason', 'full_health');
  END IF;
  IF NOT public.action_gate(uid, 'action:food', interval '2 seconds') THEN
    RETURN jsonb_build_object('ok', false, 'reason', 'too_fast');
  END IF;

  qty := coalesce((slot->>'qty')::integer, 1);
  IF qty <= 0 THEN RETURN jsonb_build_object('ok', false, 'reason', 'empty'); END IF;

  healed := least(heal, max_hp - hp);
  IF qty <= 1 THEN
    data := jsonb_set(data, ARRAY['inv', _index::text], 'null'::jsonb, true);
  ELSE
    data := jsonb_set(data, ARRAY['inv', _index::text, 'qty'], to_jsonb(qty - 1), true);
  END IF;
  data := jsonb_set(data, '{hp}', to_jsonb(hp + healed), true);
  data := jsonb_set(data, '{food}', to_jsonb(food_id), true);
  data := public.clear_stale_food(data);

  UPDATE public.player_saves SET data = data, updated_at = now() WHERE user_id = uid;
  RETURN jsonb_build_object('ok', true, 'healed', healed, 'food_used', true, 'state', public.pl_state(data));
END
$$;

REVOKE ALL ON FUNCTION public.consume_food(integer) FROM PUBLIC, anon;
GRANT EXECUTE ON FUNCTION public.consume_food(integer) TO authenticated;

-- The retired v1 helper resolved healing from the legacy public.game_items
-- table, which never carried the released dishes. It is no longer reachable
-- from the public entry point above; it stays non-public for rollback only.
REVOKE ALL ON FUNCTION public.consume_food_v1(integer) FROM PUBLIC, anon, authenticated;

DO $v7_stage_content_exit$
BEGIN
  IF (SELECT status FROM public.game_content_versions WHERE content_version = 'v7') <> 'staged' THEN
    RAISE EXCEPTION 'V7 content must remain staged after step 1';
  END IF;
  IF (SELECT manifest_hash FROM public.game_content_versions WHERE content_version = 'v7') <> '89789521c5b84948af1b954e86fdd9d6bea127d8315d3bb5b4f553d340a46935' THEN
    RAISE EXCEPTION 'V7 manifest hash was not recorded';
  END IF;
  IF (SELECT count(*) FROM public.game_content_items WHERE content_version = 'v7') <> 173 THEN
    RAISE EXCEPTION 'V7 must stage exactly 173 items';
  END IF;
  IF (SELECT count(*) FROM public.game_content_items WHERE content_version = 'v7' AND kind = 'food') <> 16 THEN
    RAISE EXCEPTION 'V7 must stage exactly 16 dishes';
  END IF;
  IF (SELECT count(*) FROM public.game_content_items
      WHERE content_version = 'v7' AND kind = 'food' AND id = ANY (ARRAY['ancient_feast', 'ascendant_feast', 'berry_pie', 'fishermans_stew', 'frost_tonic', 'golden_koi_feast', 'hearty_stew', 'honey_bun', 'phoenix_fillet', 'runic_fish_stew', 'shadow_stew', 'starsteel_feast', 'sunspiced_eel', 'void_feast', 'wyrm_feast', 'wyrmforged_feast']::text[])) <> 16 THEN
    RAISE EXCEPTION 'V7 food ids are not the approved stable ids';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'honey_bun') <> 15 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for honey_bun';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'berry_pie') <> 45 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for berry_pie';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'hearty_stew') <> 120 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for hearty_stew';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'fishermans_stew') <> 135 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for fishermans_stew';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'golden_koi_feast') <> 155 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for golden_koi_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'sunspiced_eel') <> 180 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for sunspiced_eel';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'runic_fish_stew') <> 210 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for runic_fish_stew';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'shadow_stew') <> 245 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for shadow_stew';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'frost_tonic') <> 300 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for frost_tonic';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'wyrm_feast') <> 340 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for wyrm_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'phoenix_fillet') <> 375 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for phoenix_fillet';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'starsteel_feast') <> 445 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for starsteel_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'void_feast') <> 485 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for void_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'wyrmforged_feast') <> 525 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for wyrmforged_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'ancient_feast') <> 605 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for ancient_feast';
  END IF;
  IF (SELECT heal FROM public.game_content_items
      WHERE content_version = 'v7' AND id = 'ascendant_feast') <> 645 THEN
    RAISE EXCEPTION 'V7 staged the wrong healing amount for ascendant_feast';
  END IF;
  IF EXISTS (
    SELECT 1 FROM public.game_content_items AS after
    JOIN public.game_content_items AS before
      ON before.content_version = 'v6' AND before.id = after.id
    WHERE after.content_version = 'v7'
      AND (after.name, after.value, after.level_requirement, after.tier_index, after.kind, after.stackable, after.tradable)
       IS DISTINCT FROM (before.name, before.value, before.level_requirement, before.tier_index, before.kind, before.stackable, before.tradable)
  ) THEN
    RAISE EXCEPTION 'V7 changed an item field outside the healing delta';
  END IF;
  IF EXISTS (
    SELECT 1 FROM public.game_content_items AS after
    JOIN public.game_content_items AS before
      ON before.content_version = 'v6' AND before.id = after.id
    WHERE after.content_version = 'v7'
      AND after.kind <> 'food'
      AND after.heal IS DISTINCT FROM before.heal
  ) THEN
    RAISE EXCEPTION 'V7 changed healing outside the released dishes';
  END IF;
  IF (SELECT count(*) FROM public.game_content_recipes WHERE content_version = 'v7') <> 108 THEN
    RAISE EXCEPTION 'V7 must stage exactly 108 recipes';
  END IF;
  IF (SELECT count(*) FROM public.game_content_recipe_inputs WHERE content_version = 'v7') <> 245 THEN
    RAISE EXCEPTION 'V7 recipe inputs are incomplete';
  END IF;
  IF (SELECT count(*) FROM public.game_content_recipes
      WHERE content_version = 'v7' AND output_item_id = ANY (ARRAY['ancient_feast', 'ascendant_feast', 'berry_pie', 'fishermans_stew', 'frost_tonic', 'golden_koi_feast', 'hearty_stew', 'honey_bun', 'phoenix_fillet', 'runic_fish_stew', 'shadow_stew', 'starsteel_feast', 'sunspiced_eel', 'void_feast', 'wyrm_feast', 'wyrmforged_feast']::text[]) AND skill = 'cooking') <> 16 THEN
    RAISE EXCEPTION 'V7 must stage exactly 16 Cooking recipes';
  END IF;
  IF EXISTS (
    SELECT 1
    FROM public.game_content_recipes AS recipe
    JOIN public.game_content_recipe_inputs AS input
      ON input.content_version = recipe.content_version AND input.recipe_id = recipe.id
    LEFT JOIN public.game_content_items AS ingredient
      ON ingredient.content_version = recipe.content_version AND ingredient.id = input.item_id
    WHERE recipe.content_version = 'v7'
      AND recipe.output_item_id = ANY (ARRAY['ancient_feast', 'ascendant_feast', 'berry_pie', 'fishermans_stew', 'frost_tonic', 'golden_koi_feast', 'hearty_stew', 'honey_bun', 'phoenix_fillet', 'runic_fish_stew', 'shadow_stew', 'starsteel_feast', 'sunspiced_eel', 'void_feast', 'wyrm_feast', 'wyrmforged_feast']::text[])
      AND (ingredient.id IS NULL OR NOT ingredient.active OR input.qty <= 0)
  ) THEN
    RAISE EXCEPTION 'A V7 Cooking recipe uses an undefined, inactive or empty ingredient';
  END IF;
  IF (SELECT count(*) FROM public.game_content_items WHERE content_version = 'v6') <> 173 THEN
    RAISE EXCEPTION 'V7 staging modified the v6 catalogue';
  END IF;
  IF EXISTS (
    SELECT 1 FROM public.game_content_items
    WHERE content_version = 'v6' AND kind = 'food' AND id = ANY (ARRAY['ancient_feast', 'ascendant_feast', 'berry_pie', 'fishermans_stew', 'frost_tonic', 'golden_koi_feast', 'hearty_stew', 'honey_bun', 'phoenix_fillet', 'runic_fish_stew', 'shadow_stew', 'starsteel_feast', 'sunspiced_eel', 'void_feast', 'wyrm_feast', 'wyrmforged_feast']::text[])
      AND heal NOT IN (15, 45, 120, 135, 155, 180, 210, 245, 300, 340, 650, 700, 755, 815, 880, 950)
  ) THEN
    RAISE EXCEPTION 'V7 staging modified v6 healing amounts';
  END IF;
END
$v7_stage_content_exit$;

COMMIT;