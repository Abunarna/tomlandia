# Gate 9 — isolated staging rehearsal

Status: **passed on an isolated, private staging Supabase project; no production mutation, activation, merge, deployment, or Lovable-agent prompt.**

## Rehearsal result

The staged v2 package applied successfully after restoring the locked legacy-world baseline required by Gate 7.

| Check | Result |
| --- | --- |
| v2 content version | `staged` |
| v2 canonical spawns | 730 |
| v2 UUID nodes | 369 |
| v2 UUID monsters | 361 |
| activation-control rows | 0 |
| release maintenance mode | `false` |
| preserved v1 nodes | 234 |
| preserved v1 monsters | 170 |
| preserved v1 Tungsten nodes | 17 |

## Prerequisites found and resolved in staging

1. Gate 6 required Gate 2's server-authority contracts and the immutable v1 item catalogue; these were installed in staging only.
2. Gate 7 deliberately rejected the current live-world expansion. The staging world was replaced with the locked Gate 7 legacy reset plus southern-extension migrations, yielding the required 234/170/17 baseline.

## Boundary

This is a staging rehearsal record. It does not authorize production activation, player migration, merge to `main`, publishing, or removal of the 14-day v1 observation window.
